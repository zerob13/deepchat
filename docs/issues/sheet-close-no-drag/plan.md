# Plan

- Add explicit Electron `no-drag` styling to the shared sheet close button.
- Add a focused regression check that the sheet close button keeps the `no-drag` app region.
- Run format, i18n, lint, typecheck, and the focused test.
