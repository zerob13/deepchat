# Plan

1. Add the explicit built-in provider and OpenAI-compatible runtime definition.
2. Register the existing public DaoXE logo.
3. Add focused configuration, runtime, and icon tests.
4. Run format, i18n, lint, type checks, and focused tests.
